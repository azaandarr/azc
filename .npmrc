; Internal registry configuration.
; Update the registry URL below to point to your organisation's private npm registry.
; registry=https://npm.internal.yourcompany.com/
; //npm.internal.yourcompany.com/:_authToken=${NPM_TOKEN}
